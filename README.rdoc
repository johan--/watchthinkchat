== Setup

Copy example.env to .env
Make sure operator_app_url and visitor_app_url are set to whatever domain you set up in hosts

== Tests

ALL: bundle exec rake rspec

specific test: bundle exec rspec spec/controllers/api/chats_controller_spec.rb 
